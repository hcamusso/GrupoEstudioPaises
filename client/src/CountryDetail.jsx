import React from "react";
import Country from './Country';

export default function CountryDetail() {
    
return ( 
        <Country 
            // name={nombre}
            // code={codigoPaís.id}
            // continente={city.main.temp_max}
            // capital={capital}
            // subRegion={city.main.temp_min}
            // area={city.name}
            // poblacion={city.weather[0].icon}
            // turismo={travesía}
         />
)
};
    